# app

Purpose: modular backend application packages.

Owns: API routes, orchestrator, model gateway, experts, routing, validators, telemetry, and SQLite boundary.

Entry point: `main.py`.

Tests: `../tests`.

