# AWS future seam

React build → S3 + CloudFront  
FastAPI → EC2/ECS  
Ollama models → GPU EC2  
SQLite → RDS PostgreSQL  
logs/metrics → CloudWatch  
secrets → Secrets Manager  
public API → ALB/API Gateway as appropriate

No cloud infrastructure is implemented in local v1.

