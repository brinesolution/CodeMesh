# evaluation

Purpose: reproducible route accuracy, latency, and classroom prompt checks.

Entry points: `run_routing_eval.py`, `run_latency_eval.py`.

Reports are local artifacts and are not committed by default.

