# Frontend

Purpose: polished responsive React/Vite chat interface for the local API.

Run: `npm run dev`; production check: `npm run build`; tests: `npm test`.

Status: streaming UI, routing trace, sessions, telemetry drawer, and mobile layout implemented.

